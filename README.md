# KomntJS

Annotate the web!

## Usage

Require the dist file in your HTML:

```HTML
<script src='dist/komnt.js'></script>
```

```JavaScript
window.onload = function () {
  var komnt = new Komnt();
  komnt.enable();
};
```
